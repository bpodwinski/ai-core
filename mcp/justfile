set windows-shell := ["powershell", "-NoProfile", "-Command"]

rust-docs := "servers/rust-docs"

# Build the Rust binary locally
build:
    Set-Location {{rust-docs}}; cargo build --release

# Check compilation without producing a binary
check:
    Set-Location {{rust-docs}}; cargo check

# Build Docker image (shared by mcp-leptos + mcp-leptos-use)
docker-build:
    docker compose build mcp-leptos

# Start the full stack locally
up:
    docker compose up -d --build

# Stop the stack
down:
    docker compose down

# Show logs (ex: just logs mcp-leptos)
logs *ARGS:
    docker compose logs -f {{ARGS}}

# Deploy to remote server
deploy:
    powershell -NoProfile -ExecutionPolicy Bypass -File deploy.ps1

# Build + deploy
ship: docker-build deploy

# Restart a service (ex: just restart mcp-leptos)
restart SERVICE:
    docker compose restart {{SERVICE}}
